# 민순의 요청

```text
* 역할: 너는 제미나이 캔버스 환경에 정통한 베테랑 소프트웨어 아키텍트야.
* 상황: 복잡한 설정 없이 서비스를 만들고 싶어.
* 과업: HTML, CSS, JavaScript를 별도 파일로 나누지 않고 index.html 파일 하나에 모두 담는 단일 파일 구조로 기술 스택을 확정했어. 이 구조를 가장 효율적으로 짤 수 있는 기본 뼈대 코드를 작성해 줘.
* 제약:
  - 설치 과정이 아예 없었으면 좋겠어. (Node.js, React, Build 도구 절대 사용 금지)
  - 오직 순수 HTML, CSS, Vanilla JavaScript만 사용해서 파일 하나에 다 들어가는 방식으로 짜 줘.
  - 코드에는 개발 용어 말고 비즈니스 역할을 설명하는 주석 달아 줘.
* 출력: 추천 기술 스택 목록
* 입력: [만들고 싶은 서비스 개요]
 (예시: 선물 추천 서비스)
```

# 제미나이의 응답

반갑습니다. 제미나이 캔버스 환경에서 가장 빠르고 효율적으로 프로토타이핑을 할 수 있는 **'Zero-Config, Single-File'** 아키텍처를 제안해 드립니다.

입력해주신 **[선물 추천 서비스]**를 예시로, 복잡한 도구 없이 즉시 실행 가능한 최적의 뼈대 코드를 작성했습니다.

### 🏗️ 추천 기술 스택 (The "Vanilla" Stack)

이 구조는 외부 의존성이 0%이므로, 인터넷이 끊겨도 브라우저만 있으면 돌아갑니다.

1.  **구조 (Structure):** HTML5 (시맨틱 태그 사용)
2.  **스타일 (Style):** CSS3 (Flexbox/Grid, CSS Variables 활용)
3.  **로직 (Logic):** Vanilla JavaScript (ES6+ 문법)
4.  **데이터 (Data):** 인메모리 객체 (필요 시 LocalStorage로 브라우저 저장 가능)

---

### 📄 단일 파일 코드 (index.html)

아래 코드를 복사해서 `index.html`로 저장하고 브라우저에서 열기만 하면 됩니다.

```html
<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>선물 추천 서비스 MVP</title>
    
    <!-- [비즈니스 영역: 브랜드 디자인 및 스타일링] -->
    <style>
        :root {
            /* 브랜드 컬러 팔레트 정의 */
            --primary-color: #6200ea;
            --secondary-color: #03dac6;
            --bg-color: #f5f5f5;
            --text-color: #333;
            --card-bg: #ffffff;
        }

        * { box-sizing: border-box; margin: 0; padding: 0; }

        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
            background-color: var(--bg-color);
            color: var(--text-color);
            line-height: 1.6;
            display: flex;
            justify-content: center;
            min-height: 100vh;
        }

        /* 서비스 메인 컨테이너 (모바일 친화적 폭 설정) */
        #app-container {
            width: 100%;
            max-width: 600px;
            background: var(--card-bg);
            padding: 2rem;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
        }

        header {
            text-align: center;
            margin-bottom: 2rem;
            border-bottom: 2px solid var(--primary-color);
            padding-bottom: 1rem;
        }

        h1 { color: var(--primary-color); font-size: 1.8rem; }
        p.subtitle { color: #666; font-size: 0.9rem; }

        /* 입력 폼 영역 */
        .input-section {
            display: flex;
            flex-direction: column;
            gap: 1rem;
            margin-bottom: 2rem;
        }

        label { font-weight: bold; font-size: 0.9rem; }
        
        select, input, button {
            padding: 0.8rem;
            border-radius: 8px;
            border: 1px solid #ddd;
            font-size: 1rem;
        }

        /* 주요 액션 버튼 (CTA) */
        button.primary-btn {
            background-color: var(--primary-color);
            color: white;
            border: none;
            cursor: pointer;
            font-weight: bold;
            transition: background 0.3s;
        }

        button.primary-btn:hover { background-color: #3700b3; }

        /* 결과 표시 영역 */
        #result-area {
            background-color: #f0f0f0;
            padding: 1.5rem;
            border-radius: 8px;
            display: none; /* 초기에는 숨김 */
        }

        .recommendation-card {
            border-left: 4px solid var(--secondary-color);
            padding-left: 1rem;
            margin-top: 1rem;
        }
    </style>
</head>
<body>

    <!-- [비즈니스 영역: 고객 접점 화면 구성] -->
    <div id="app-container">
        <header>
            <h1>🎁 기프트 마스터</h1>
            <p class="subtitle">당신의 소중한 사람을 위한 완벽한 선물 찾기</p>
        </header>

        <main>
            <!-- 고객 요구사항 수집 섹션 -->
            <section class="input-section">
                <div>
                    <label for="relation">받는 분과의 관계</label>
                    <select id="relation">
                        <option value="">선택해주세요</option>
                        <option value="lover">연인</option>
                        <option value="parent">부모님</option>
                        <option value="friend">친구</option>
                        <option value="colleague">직장동료</option>
                    </select>
                </div>

                <div>
                    <label for="budget">예산 범위 (만원)</label>
                    <input type="number" id="budget" placeholder="예: 5" min="0">
                </div>

                <button id="recommend-btn" class="primary-btn">선물 추천 받기</button>
            </section>

            <!-- 서비스 결과 제공 섹션 -->
            <section id="result-area">
                <h3>추천 결과</h3>
                <div id="result-content"></div>
            </section>
        </main>
    </div>

    <!-- [비즈니스 영역: 서비스 로직 및 데이터 처리] -->
    <script>
        /**
         * 서비스 데이터베이스 (Mock Data)
         * 실제 운영 시에는 서버 API에서 가져올 데이터를 시뮬레이션 함
         */
        const GIFT_DATABASE = [
            { id: 1, name: "고급 와인 세트", price: 5, target: ["parent", "colleague", "friend"] },
            { id: 2, name: "커플 파자마", price: 8, target: ["lover"] },
            { id: 3, name: "홍삼 스틱", price: 10, target: ["parent"] },
            { id: 4, name: "무선 이어폰 케이스", price: 3, target: ["friend", "lover"] },
            { id: 5, name: "데스크 오거나이저", price: 4, target: ["colleague"] },
            { id: 6, name: "호텔 뷔페 식사권", price: 15, target: ["parent", "lover"] },
            { id: 7, name: "핸드크림 세트", price: 2, target: ["friend", "colleague"] }
        ];

        /**
         * 비즈니스 로직 컨트롤러
         * 화면 요소와 데이터를 연결하는 역할
         */
        const AppController = {
            // UI 요소 캐싱 (DOM 접근 최소화)
            ui: {
                relationSelect: document.getElementById('relation'),
                budgetInput: document.getElementById('budget'),
                recommendBtn: document.getElementById('recommend-btn'),
                resultArea: document.getElementById('result-area'),
                resultContent: document.getElementById('result-content')
            },

            // 초기화 함수: 서비스 시작 시 실행
            init() {
                console.log("서비스가 정상적으로 로드되었습니다.");
                this.bindEvents();
            },

            // 고객 행동 감지 (이벤트 리스너)
            bindEvents() {
                this.ui.recommendBtn.addEventListener('click', () => {
                    this.handleRecommendationRequest();
                });
            },

            // 핵심 비즈니스 로직: 추천 알고리즘 수행
            handleRecommendationRequest() {
                const relation = this.ui.relationSelect.value;
                const budget = parseInt(this.ui.budgetInput.value);

                // 유효성 검사: 고객이 필수 정보를 입력했는지 확인
                if (!relation || isNaN(budget)) {
                    alert("관계와 예산을 모두 입력해주세요!");
                    return;
                }

                // 필터링 로직: 조건에 맞는 상품 검색
                const recommendations = GIFT_DATABASE.filter(item => {
                    const isTargetMatch = item.target.includes(relation);
                    const isBudgetMatch = item.price <= budget;
                    return isTargetMatch && isBudgetMatch;
                });

                // 결과 렌더링: 화면에 결과 표시
                this.renderResults(recommendations);
            },

            // 화면 업데이트 로직
            renderResults(items) {
                this.ui.resultArea.style.display = 'block';
                
                if (items.length === 0) {
                    this.ui.resultContent.innerHTML = `<p>조건에 맞는 선물을 찾지 못했습니다. 😢<br>예산을 조금 더 올려보시는 건 어떨까요?</p>`;
                    return;
                }

                // 결과 리스트 생성
                const html = items.map(item => `
                    <div class="recommendation-card">
                        <h4>${item.name}</h4>
                        <p>가격: ${item.price}만원</p>
                    </div>
                `).join('');

                this.ui.resultContent.innerHTML = html;
            }
        };

        // 서비스 실행
        document.addEventListener('DOMContentLoaded', () => {
            AppController.init();
        });
    </script>
</body>
</html>
```

### 💡 아키텍트의 조언 (사용 가이드)

1.  **데이터 확장:** `GIFT_DATABASE` 배열 안에 상품 정보를 JSON 형태로 계속 추가하면 됩니다.
2.  **디자인 변경:** `<style>` 태그 내의 `:root` 변수(`--primary-color` 등)만 수정하면 전체 테마 색상을 한 번에 바꿀 수 있습니다.
3.  **배포:** 이 파일 하나만 있으면 GitHub Pages나 Netlify Drop 같은 곳에 드래그 앤 드롭하여 10초 만에 전 세계에 배포할 수 있습니다.

이 코드는 제미나이 캔버스에서 바로 수정하고 미리보기를 하기에 최적화되어 있습니다.