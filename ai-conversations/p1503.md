# 민순의 요청

```text
* 역할: 너는 단일 파일 HTML 개발 전문가야.
* 상황: 나는 이미 검증된 '기본형 추천 서비스 HTML 템플릿'을 가지고 있어.
* 과업: 이 템플릿을 기반으로 입력된 새로운 주제의 추천 서비스를 구현해 줘.
* 제약: 
  - 기존 스크립트의 추천 로직은 건드리지 마. 
  - 화면에 나오는 질문과 상품 목록만 새로운 주제에 맞게 바꿔 줘.
  - 수정된 부분에는 비즈니스 용어로 주석을 달아 줘.
* 출력: 수정된 index.html 전체 코드.
* 입력: [기본형 템플릿 코드 및 새 주제 설명]
 (예시: 템플릿 파일 첨부, 주제는 결혼식 답례품 추천)
```

# 제미나이의 응답

네, 알겠습니다. **단일 파일 HTML 개발 전문가**로서 요청하신 과업을 수행하겠습니다.

현재 입력란에 구체적인 **[기본형 템플릿 코드]**가 포함되지 않았으므로, 제가 통상적으로 사용하는 **표준 추천 로직(가중치 점수 합산 방식)**을 가진 템플릿이 있다고 가정하고, 예시로 들어주신 **[결혼식 답례품 추천]** 주제를 적용하여 코드를 작성해 드리겠습니다.

만약 가지고 계신 특정 템플릿 코드가 있다면, 그 코드를 붙여넣어 주시면 해당 코드의 로직을 유지한 채 데이터만 변경해 드리겠습니다.

아래는 **표준 템플릿**에 **결혼식 답례품** 데이터를 적용한 전체 코드입니다.

```html
<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>결혼식 답례품 추천 서비스</title>
    <style>
        /* 스타일은 주제에 맞게 깔끔한 웨딩 테마로 적용 */
        body { font-family: 'Noto Sans KR', sans-serif; background-color: #f9f9f9; display: flex; justify-content: center; align-items: center; min-height: 100vh; margin: 0; }
        .container { background: white; padding: 2rem; border-radius: 15px; box-shadow: 0 4px 15px rgba(0,0,0,0.1); width: 100%; max-width: 500px; text-align: center; }
        h1 { color: #333; margin-bottom: 1.5rem; font-size: 1.5rem; }
        .btn { display: block; width: 100%; padding: 12px; margin: 10px 0; background-color: #e8a6a6; color: white; border: none; border-radius: 8px; cursor: pointer; font-size: 1rem; transition: 0.3s; }
        .btn:hover { background-color: #d68c8c; }
        .hidden { display: none; }
        .result-box { margin-top: 20px; padding: 15px; border: 1px solid #eee; border-radius: 8px; background-color: #fff5f5; }
        .product-img { width: 100%; height: 200px; object-fit: cover; border-radius: 8px; margin-bottom: 10px; background-color: #ddd; }
        .restart-btn { background-color: #555; margin-top: 20px; }
    </style>
</head>
<body>

<div class="container">
    <!-- 시작 화면 -->
    <div id="start-screen">
        <h1>💍 결혼식 답례품 추천</h1>
        <p>하객분들을 위한 최고의 선물을 찾아드려요.</p>
        <button class="btn" onclick="startService()">추천 받기 시작</button>
    </div>

    <!-- 질문 화면 -->
    <div id="question-screen" class="hidden">
        <h2 id="question-text">질문 내용</h2>
        <div id="answer-buttons"></div>
    </div>

    <!-- 결과 화면 -->
    <div id="result-screen" class="hidden">
        <h2>추천 결과</h2>
        <div id="result-content"></div>
        <button class="btn restart-btn" onclick="location.reload()">다시 하기</button>
    </div>
</div>

<script>
    /**
     * ============================================================
     * [영역 1] 비즈니스 데이터 설정 (수정 가능 영역)
     * 주제: 결혼식 답례품 추천
     * 작성자: HTML 전문가
     * ============================================================
     */

    // [비즈니스 용어] 추천 상품 데이터베이스 (Product Catalog)
    // type: 매칭 로직에서 사용될 카테고리 식별자
    const products = [
        {
            id: 1,
            name: "프리미엄 소금 세트",
            desc: "호불호가 없고 고급스러운 실속형 선물입니다.",
            type: "utility", // 실용성 중시
            priceLevel: "low",
            img: "https://via.placeholder.com/400x200?text=Premium+Salt"
        },
        {
            id: 2,
            name: "수제 쿠키 & 마들렌 박스",
            desc: "젊은 층과 어린이 하객에게 인기 만점인 달콤한 선물입니다.",
            type: "food", // 맛/간식 중시
            priceLevel: "mid",
            img: "https://via.placeholder.com/400x200?text=Cookies"
        },
        {
            id: 3,
            name: "고급 전통주 (미니)",
            desc: "어르신들이 좋아하시며 격식 있는 자리에 어울립니다.",
            type: "alcohol", // 어르신/주류
            priceLevel: "mid",
            img: "https://via.placeholder.com/400x200?text=Traditional+Wine"
        },
        {
            id: 4,
            name: "호텔급 타월 세트",
            desc: "가장 무난하면서도 질 좋은 수건은 언제나 환영받습니다.",
            type: "utility", // 실용성 중시
            priceLevel: "mid",
            img: "https://via.placeholder.com/400x200?text=Luxury+Towel"
        },
        {
            id: 5,
            name: "천연 핸드워시 & 핸드크림",
            desc: "센스 있고 향기로운 선물로 기억에 남습니다.",
            type: "beauty", // 감성/뷰티
            priceLevel: "high",
            img: "https://via.placeholder.com/400x200?text=Hand+Wash"
        }
    ];

    // [비즈니스 용어] 설문 시나리오 (Question Flow)
    // answers의 type은 위 상품의 type과 매칭되어 점수를 부여함
    const questions = [
        {
            q: "가장 중요하게 생각하는 예산 범위는?",
            answers: [
                { text: "가성비 (5천원 이하)", scoreType: "low" }, // 가격대 매칭용
                { text: "적당함 (5천원 ~ 1만원)", scoreType: "mid" },
                { text: "고급스러움 (1만원 이상)", scoreType: "high" }
            ]
        },
        {
            q: "주로 오시는 하객분들의 연령대는?",
            answers: [
                { text: "젊은 친구/동료 위주", scoreType: "food" }, // 간식 선호
                { text: "가족/어르신 위주", scoreType: "alcohol" }, // 전통주 선호
                { text: "남녀노소 다양함", scoreType: "utility" } // 실용성 선호
            ]
        },
        {
            q: "선물의 분위기는 어땠으면 하나요?",
            answers: [
                { text: "실용적인게 최고", scoreType: "utility" },
                { text: "감성적이고 예쁜 것", scoreType: "beauty" },
                { text: "맛있는게 남는 것", scoreType: "food" }
            ]
        }
    ];

    /**
     * ============================================================
     * [영역 2] 추천 엔진 로직 (수정 금지 영역)
     * Core Logic: 점수 계산 및 결과 렌더링
     * ============================================================
     */
    
    let currentStep = 0;
    let userScores = {}; // 사용자가 선택한 타입별 점수 누적

    function startService() {
        document.getElementById('start-screen').classList.add('hidden');
        document.getElementById('question-screen').classList.remove('hidden');
        showQuestion();
    }

    function showQuestion() {
        const qData = questions[currentStep];
        document.getElementById('question-text').innerText = qData.q;
        const btnContainer = document.getElementById('answer-buttons');
        btnContainer.innerHTML = '';

        qData.answers.forEach(ans => {
            const btn = document.createElement('button');
            btn.className = 'btn';
            btn.innerText = ans.text;
            btn.onclick = () => handleAnswer(ans.scoreType);
            btnContainer.appendChild(btn);
        });
    }

    function handleAnswer(type) {
        // 로직: 선택한 타입에 점수 +1
        if (!userScores[type]) userScores[type] = 0;
        userScores[type]++;

        currentStep++;
        if (currentStep < questions.length) {
            showQuestion();
        } else {
            showResult();
        }
    }

    function showResult() {
        document.getElementById('question-screen').classList.add('hidden');
        document.getElementById('result-screen').classList.remove('hidden');

        // 로직: 가장 점수가 높은 타입 또는 가격대가 일치하는 상품 찾기 (단순화된 매칭 로직)
        // 실제로는 더 복잡할 수 있으나, 템플릿 로직을 따른다고 가정.
        // 여기서는 가장 많이 선택된 type을 가진 상품 중 하나를 추천하거나, 
        // 점수 매칭이 가장 잘 되는 상품을 필터링.
        
        // 1. 가장 높은 점수의 키 찾기
        let maxScore = 0;
        let bestType = '';
        for (const [key, value] of Object.entries(userScores)) {
            if (value > maxScore) {
                maxScore = value;
                bestType = key;
            }
        }

        // 2. 해당 타입(또는 가격대)과 일치하는 상품 필터링
        // (가격대와 카테고리가 섞여 있으므로 둘 중 하나라도 매칭되면 후보군에 포함)
        const recommendations = products.filter(p => 
            p.type === bestType || p.priceLevel === bestType
        );

        // 매칭된게 없으면 전체 중 랜덤, 있으면 그 중 랜덤
        const finalCandidates = recommendations.length > 0 ? recommendations : products;
        const finalProduct = finalCandidates[Math.floor(Math.random() * finalCandidates.length)];

        // 결과 렌더링
        const resultDiv = document.getElementById('result-content');
        resultDiv.innerHTML = `
            <div class="result-box">
                <img src="${finalProduct.img}" alt="${finalProduct.name}" class="product-img">
                <h3>${finalProduct.name}</h3>
                <p>${finalProduct.desc}</p>
            </div>
        `;
    }
</script>

</body>
</html>
```