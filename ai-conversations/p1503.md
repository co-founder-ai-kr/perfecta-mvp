# 민순의 요청

```text
* 역할: 너는 단일 파일 HTML 개발 전문가야.
* 상황: 나는 이미 검증된 '기본형 추천 서비스 HTML 템플릿'을 가지고 있어.
* 과업: 이 템플릿을 기반으로 '결혼식 답례품 추천 서비스'를 구현해 줘.
* 제약: 
  - 기존 스크립트의 추천 로직은 건드리지 마. 
  - 화면에 나오는 질문과 상품 목록만 '결혼식 답례품'에 맞게 바꿔 줘.
  - 수정된 부분에는 비즈니스 용어로 주석을 달아 줘.
* 출력: 수정된 index.html 전체 코드.
* 입력: [기본형 템플릿 코드 파일 첨부 및 새 주제 설명]
 (예시: 결혼식 답례품 추천 서비스)
```

# 제미나이의 응답

제공해주신 기본형 템플릿 코드가 텍스트로 포함되지 않았으므로, **가장 보편적이고 확장성 높은 '태그 매칭 방식'의 단일 파일 HTML 템플릿**을 기준으로 작성했습니다.

요청하신 대로 **추천 로직(엔진)은 건드리지 않고**, **데이터 영역(질문, 상품)**만 '결혼식 답례품' 주제에 맞춰 수정했습니다. 수정된 부분에는 비즈니스 용어 주석을 달았습니다.

이 코드를 그대로 `.html` 파일로 저장하여 실행하시면 됩니다.

```html
<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>결혼식 답례품 추천 서비스</title>
    <style>
        /* 스타일 영역: 웨딩 테마에 맞춰 깔끔하고 화사하게 구성 */
        :root {
            --primary-color: #ff9a9e;
            --secondary-color: #fad0c4;
            --text-color: #333;
            --bg-color: #f9f9f9;
        }
        body {
            font-family: 'Pretendard', -apple-system, BlinkMacSystemFont, system-ui, Roboto, sans-serif;
            background-color: var(--bg-color);
            color: var(--text-color);
            margin: 0;
            padding: 20px;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }
        .container {
            background: white;
            width: 100%;
            max-width: 500px;
            padding: 30px;
            border-radius: 20px;
            box-shadow: 0 10px 25px rgba(0,0,0,0.1);
            text-align: center;
        }
        h1 { color: var(--primary-color); margin-bottom: 10px; }
        .desc { color: #666; margin-bottom: 30px; font-size: 0.95rem; }
        
        /* 진행바 */
        .progress-bar {
            width: 100%;
            height: 6px;
            background-color: #eee;
            border-radius: 3px;
            margin-bottom: 30px;
            overflow: hidden;
        }
        .progress-fill {
            height: 100%;
            background: linear-gradient(to right, var(--secondary-color), var(--primary-color));
            width: 0%;
            transition: width 0.3s ease;
        }

        /* 질문 및 버튼 */
        .question-box { display: none; }
        .question-box.active { display: block; animation: fadeIn 0.5s; }
        .q-text { font-size: 1.2rem; font-weight: bold; margin-bottom: 20px; }
        
        .btn-option {
            display: block;
            width: 100%;
            padding: 15px;
            margin-bottom: 10px;
            border: 1px solid #ddd;
            border-radius: 10px;
            background: white;
            cursor: pointer;
            transition: all 0.2s;
            font-size: 1rem;
        }
        .btn-option:hover {
            background-color: var(--secondary-color);
            border-color: var(--primary-color);
            color: white;
            font-weight: bold;
        }

        /* 결과 화면 */
        .result-box { display: none; }
        .product-card {
            border: 1px solid #eee;
            border-radius: 15px;
            padding: 20px;
            margin-bottom: 15px;
            background: #fff;
            text-align: left;
            box-shadow: 0 4px 6px rgba(0,0,0,0.05);
        }
        .product-name { font-size: 1.1rem; font-weight: bold; color: #333; }
        .product-desc { font-size: 0.9rem; color: #666; margin-top: 5px; }
        .product-tag {
            display: inline-block;
            background: #eee;
            padding: 3px 8px;
            border-radius: 4px;
            font-size: 0.75rem;
            margin-top: 10px;
            color: #555;
        }
        .btn-restart {
            background-color: var(--primary-color);
            color: white;
            border: none;
            padding: 15px 30px;
            border-radius: 30px;
            font-size: 1rem;
            cursor: pointer;
            margin-top: 20px;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }
    </style>
</head>
<body>

<div class="container">
    <!-- 헤더 영역 -->
    <h1>💍 답례품 추천</h1>
    <p class="desc">하객분들을 위한 최고의 선물을 찾아드려요.</p>
    
    <!-- 진행바 -->
    <div class="progress-bar">
        <div class="progress-fill" id="progress"></div>
    </div>

    <!-- 질문 영역 (JS로 주입됨) -->
    <div id="quiz-container"></div>

    <!-- 결과 영역 -->
    <div id="result-container" class="result-box">
        <h2>추천 결과</h2>
        <p style="margin-bottom:20px;">가장 적합한 답례품 TOP 3입니다.</p>
        <div id="product-list"></div>
        <button class="btn-restart" onclick="location.reload()">다시 하기</button>
    </div>
</div>

<script>
    /**
     * =================================================================
     * [DATA SECTION] - 수정 가능 영역
     * 비즈니스 요구사항: 결혼식 답례품 추천을 위한 데이터셋 구성
     * =================================================================
     */

    // [비즈니스 용어] Questions: 고객 니즈 파악을 위한 설문 문항
    const questions = [
        {
            id: 1,
            text: "가장 중요하게 생각하는 예산 범위는?", // [수정] 예산 관련 질문
            options: [
                { text: "가성비 최고 (5천원 미만)", tags: ['low_price', 'bulk'] },
                { text: "적당한 가격 (5천원 ~ 1만원)", tags: ['mid_price', 'standard'] },
                { text: "고급스럽게 (1만원 이상)", tags: ['high_price', 'premium'] }
            ]
        },
        {
            id: 2,
            text: "주로 어떤 하객분들에게 드리나요?", // [수정] 타겟 오디언스 질문
            options: [
                { text: "직장 동료나 친구 (젊은 층)", tags: ['young', 'trendy'] },
                { text: "친척이나 어르신 (중장년 층)", tags: ['elder', 'traditional'] },
                { text: "남녀노소 모두 (전 연령)", tags: ['all_ages', 'safe'] }
            ]
        },
        {
            id: 3,
            text: "선호하는 선물의 유형은?", // [수정] 상품 카테고리 선호도 질문
            options: [
                { text: "맛있는 게 최고 (식품류)", tags: ['food', 'snack'] },
                { text: "오래 쓰는 실용성 (생활용품)", tags: ['practical', 'living'] },
                { text: "감성적이고 예쁜 것 (디자인)", tags: ['design', 'scent'] }
            ]
        }
    ];

    // [비즈니스 용어] Product Catalog: 추천될 상품 목록 및 속성 매핑
    const products = [
        {
            name: "프리미엄 소금 & 설탕 세트", // [수정] 상품명
            desc: "호불호 없는 실속형 선물, 고급스러운 패키징.", // [수정] 상품 설명
            tags: ['low_price', 'all_ages', 'food', 'safe'] // [수정] 매칭 태그
        },
        {
            name: "호텔급 170g 고중량 수건",
            desc: "가장 전통적이고 실용적인 답례품의 정석.",
            tags: ['mid_price', 'all_ages', 'practical', 'standard']
        },
        {
            name: "수제 쿠키 & 마들렌 박스",
            desc: "젊은 하객들이 좋아하는 달콤한 디저트 세트.",
            tags: ['low_price', 'young', 'food', 'trendy']
        },
        {
            name: "핸드워시 & 핸드크림 세트",
            desc: "센스 있는 향기와 감성적인 디자인.",
            tags: ['mid_price', 'young', 'design', 'scent']
        },
        {
            name: "국산 참기름 & 들기름 세트",
            desc: "어르신들이 특히 좋아하시는 알찬 구성.",
            tags: ['high_price', 'elder', 'food', 'traditional']
        },
        {
            name: "고급 혼합 잡곡 3종",
            desc: "건강을 생각하는 마음을 담은 답례품.",
            tags: ['low_price', 'elder', 'food', 'bulk']
        },
        {
            name: "미니 더치커피 & 머그잔",
            desc: "직장 동료들에게 인기 만점인 카페인 충전 세트.",
            tags: ['mid_price', 'young', 'food', 'trendy']
        },
        {
            name: "프리미엄 꿀 스틱",
            desc: "간편하게 건강을 챙길 수 있는 고급 선물.",
            tags: ['high_price', 'all_ages', 'food', 'premium']
        },
        {
            name: "천연 비누 2구 세트",
            desc: "환경을 생각하는 착한 성분과 예쁜 모양.",
            tags: ['low_price', 'all_ages', 'practical', 'design']
        }
    ];

    /**
     * =================================================================
     * [LOGIC SECTION] - 수정 금지 영역
     * 기존 템플릿의 핵심 로직 (상태 관리, 점수 계산, 렌더링)
     * =================================================================
     */
    
    let currentStep = 0;
    let userTags = []; // 사용자가 선택한 태그들이 누적됨

    const quizContainer = document.getElementById('quiz-container');
    const resultContainer = document.getElementById('result-container');
    const productList = document.getElementById('product-list');
    const progressBar = document.getElementById('progress');

    // 초기화: 첫 번째 질문 렌더링
    function init() {
        renderQuestion();
    }

    // 질문 렌더링 함수
    function renderQuestion() {
        const q = questions[currentStep];
        
        // 진행바 업데이트
        const progressPercent = ((currentStep) / questions.length) * 100;
        progressBar.style.width = `${progressPercent}%`;

        // HTML 생성
        let html = `
            <div class="question-box active">
                <div class="q-text">Q${currentStep + 1}. ${q.text}</div>
                <div class="options">
        `;

        q.options.forEach((opt, index) => {
            html += `<button class="btn-option" onclick="handleSelect(${index})">${opt.text}</button>`;
        });

        html += `
                </div>
            </div>
        `;

        quizContainer.innerHTML = html;
    }

    // 선택 처리 함수
    window.handleSelect = function(optionIndex) {
        // 선택한 옵션의 태그를 사용자 태그 목록에 추가
        const selectedTags = questions[currentStep].options[optionIndex].tags;
        userTags = [...userTags, ...selectedTags];

        currentStep++;

        if (currentStep < questions.length) {
            renderQuestion();
        } else {
            showResult();
        }
    };

    // 결과 계산 및 출력 함수
    function showResult() {
        // 진행바 100%
        progressBar.style.width = '100%';
        
        // 질문 영역 숨기기
        quizContainer.style.display = 'none';
        resultContainer.style.display = 'block';

        // 추천 알고리즘: 태그 매칭 점수 계산
        const scoredProducts = products.map(product => {
            let score = 0;
            product.tags.forEach(tag => {
                if (userTags.includes(tag)) {
                    score++;
                }
            });
            return { ...product, score };
        });

        // 점수 내림차순 정렬
        scoredProducts.sort((a, b) => b.score - a.score);

        // 상위 3개만 추출
        const topProducts = scoredProducts.slice(0, 3);

        // 결과 렌더링
        let resultHtml = '';
        topProducts.forEach(p => {
            resultHtml += `
                <div class="product-card">
                    <div class="product-name">${p.name}</div>
                    <div class="product-desc">${p.desc}</div>
                    <div class="product-tag">#${p.tags.join(' #')}</div>
                </div>
            `;
        });

        productList.innerHTML = resultHtml;
    }

    // 앱 시작
    init();

</script>
</body>
</html>
```